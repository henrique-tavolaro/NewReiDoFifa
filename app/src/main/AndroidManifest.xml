<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.henriquetavolaro.newreidofifa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewReiDoFifa">
        <activity android:name=".ui.activities.LoginActivity"></activity>
        <activity android:name=".ui.activities.SignUpActivity" />
        <activity android:name=".ui.activities.SignInActivity" />
        <activity android:name=".ui.activities.SplashActivity"
            android:theme="@style/Theme.NewReiDoFifa.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NewReiDoFifa.NoActionBar">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>